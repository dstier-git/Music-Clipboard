# Required dependencies for MuseScore Pitch Extractor
# Core dependencies (required)
# Note: tkinter, xml.etree.ElementTree, and zipfile are part of Python standard library

# Optional dependencies for automation features
pywinauto>=0.6.8  # For finding and activating MuseScore window on Windows
pyautogui>=0.9.54  # For sending keyboard shortcuts
keyboard>=0.13.5  # For global keyboard shortcuts (allows triggering from anywhere)
pywin32>=300  # Optional: For Windows API fallback methods (install with: pip install pywin32)
psutil>=5.9.0  # Optional: For better process detection (install with: pip install psutil)

# Optional dependencies for MIDI export
# Note: MIDI export will try to use MuseScore CLI first (if installed)
# If MuseScore CLI is not available, one of these libraries is required:
mido>=1.2.10  # Optional: For MIDI file creation (install with: pip install mido)
# music21>=8.0.0  # Alternative: For MIDI export (install with: pip install music21)

